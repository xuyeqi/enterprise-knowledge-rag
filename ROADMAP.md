# 项目路线图

## 当前阶段

阶段 5：简历亮点增强（并行补齐阶段 4 必要工程能力）

当前目标：补齐 RAG 评估与必要错误处理，形成可投递版本。

## 已完成

- 2026-07-09：确定项目方向为企业知识库 RAG 问答系统。
- 2026-07-09：确定技术栈为 Vue3 + TypeScript、FastAPI + LangChain、PostgreSQL + pgvector。
- 2026-07-09：补充项目规范文档、路线图和项目说明。
- 2026-07-09：补充第二步后端骨架的新手向文件说明和代码注释。
- 2026-07-09：统一后端依赖管理方式为 uv，并修正安装、测试和启动命令文档。
- 2026-07-09：完成后端最小骨架，包含 FastAPI 应用、`/health` 接口和健康检查测试；已通过 `uv run pytest` 验证。
- 2026-07-10：启动 PostgreSQL + pgvector 容器，并通过 VS Code 插件验证 `rag_db` 可以连接。
- 2026-07-10：完成异步数据库配置、SQLAlchemy 引擎和 `/health/db` 接口；用户执行 `uv run pytest` 两次均为 `2 passed, 1 warning`，并确认真实接口返回 `{"status":"ok","database":"connected"}`。
- 2026-07-10：阶段 1 项目初始化完成，进入阶段 2 最小 RAG 闭环。
- 2026-07-11：完成 Alembic 迁移机制、`vector` 扩展、`documents` 和 `document_chunks` 表；用户执行迁移后确认数据库出现两张业务表和 `alembic_version` 版本表。
- 2026-07-11：完成百炼 `text-embedding-v4` 异步调用服务；离线测试通过，真实 API 返回 1024 维向量。
- 2026-07-11：完成 LangChain 中文递归文本切片服务；用户执行 pytest，切片测试及现有测试全部通过。
- 2026-07-13：完成 `POST /documents/preview`；pytest 全部通过，并通过 Swagger 上传真实 txt／md 文件验证切片预览。
- 2026-07-14：完成 `POST /documents` 正式向量入库；pytest 已通过，数据库截图确认一条文档被切成 6 个片段，片段文本、顺序和 embedding 均已写入 PostgreSQL + pgvector。
- 2026-07-14：完成 `POST /search` 最小向量检索接口；问题校验、问题向量化、pgvector 余弦距离查询和来源响应的自动化测试均已通过。
- 2026-07-14：完成 `POST /answer` LangChain 知识库问答接口；依赖同步、离线测试、现有回归测试和真实 `qwen3.7-plus` 问答均已验证正常，答案能够返回引用来源。
- 2026-07-14：阶段 2 最小 RAG 闭环完成，进入阶段 3 前端最小界面。
- 2026-07-14：完成 Vue3 + TypeScript + Vite 前端初始化和健康状态页；依赖安装、生产构建、页面访问及 `/api/health` 代理链路均已验证通过。
- 2026-07-14：完成文档上传页面；文件选择、前端校验、`POST /documents` 调用和入库结果展示已通过真实文件上传验证。
- 2026-07-14：完成 `GET /documents` 和前端文档列表页；自动化测试、生产构建和真实数据展示均已验证通过。
- 2026-07-14：完成聊天问答与引用来源页面；生产构建、真实模型答案和引用切片展示均已验证通过。
- 2026-07-14：阶段 3 前端最小界面完成，进入阶段 4 工程化增强。
- 2026-07-15：完成品牌标题、Logo、天然石产品图、回答称呼、来源布局和公司知识文档；用户确认生产构建、页面视觉、真实上传与问答均验证通过。
- 2026-07-15：完成最近五轮页面级对话历史；后端测试、前端构建和连续追问均已验证通过。
- 2026-07-15：完成初始相似度阈值和无相关资料拒答；后端测试、相关问题回答及无关问题拒答均已验证通过。
- 2026-07-16：完成 SSE 流式回答、前端逐块渲染和最新回答展示；后端测试、前端构建、真实流式内容、引用来源和界面状态均已验证通过。

## 进行中

- 阶段 5：简历亮点增强，阶段 4 的必要错误处理和测试继续并行补齐。
- 后端最小 RAG 闭环已经完成并通过离线与真实链路验证。
- 前端文档上传、文档列表、聊天问答和引用来源展示已经完成并通过真实链路验证。
- 前端品牌展示和公司知识内容已经完成并通过真实验证。
- 最近五轮页面级对话历史已经完成并通过验证。
- 检索相似度阈值和无相关资料拒答策略已经完成并通过验证。
- PDF 文档解析、逐页切片、页码存储和前端来源页码展示已经完成并通过验证。
- SSE 流式回答、前端逐块渲染和最新回答展示已经完成并通过验证。

## 待办

### 阶段 4：工程化增强

- 增加基础错误处理。
- 补齐关键链路后端测试。

### 阶段 5：简历亮点增强

- PDF 解析完成后，引入 Redis＋后台任务队列解耦文档上传与向量化处理，支持异步索引、任务状态查询、失败重试和重复任务控制。
- 增加检索参数调试。
- 增加简单评估样例。
- 视情况引入 LangGraph 编排问答流程。

## 阻塞

- 暂无。

## 最近验证

- 2026-07-16：用户确认流式回答验证完成；`POST /answer/stream` 能持续返回 `delta`，前端能够逐块渲染最新答案，并在结束后展示来源和完成状态。
- 2026-07-16：新增 `POST /answer/stream`、LangChain `astream()`、SSE 增量与来源事件、前端 `ReadableStream` 解析、生成中断状态和仅展示最新回答的界面；尚未执行 pytest、前端构建或完整真实流式问答验证，因此暂未标记完成。
- 2026-07-16：用户确认 PDF 依赖同步、数据库迁移、后端测试、前端构建和真实功能链路均验证通过；文本型 PDF 可以逐页解析、向量入库，并在问答来源中展示正确页码。
- 2026-07-15：新增文本型 PDF 解析、逐页切片、可空页码迁移、引用页码展示和离线测试；尚未执行依赖同步、数据库迁移、pytest、前端构建或真实 PDF 上传，因此暂未标记完成。
- 2026-07-15：用户确认初始相似度阈值 `0.5` 通过测试；低相关切片不会进入模型上下文或来源列表，全部切片低于阈值时直接拒答并跳过聊天模型。
- 2026-07-15：用户确认最近五轮页面级对话历史通过测试；前端可保留和清空问答上下文，后端能够校验历史、增强检索并把完整历史交给生成提示词。
- 2026-07-15：用户确认品牌标题、Logo、天然石产品图、回答称呼、来源布局、正文隐藏 `[资料N]` 和公司知识文档的测试、构建及真实链路验证均通过。
- 2026-07-14：用户确认聊天问答页面的 `npm run build`、真实模型答案和引用来源展示均验证通过；阶段 3 前端最小界面完成。
- 2026-07-14：新增聊天问答 API、`/chat` 页面、问题校验、模型答案和引用来源展示；尚未执行 `npm run build` 或真实问题验证，因此暂未标记完成。
- 2026-07-14：用户确认文档列表后端测试、`npm run build` 和真实数据库文档展示均验证通过；文档列表功能完成。
- 2026-07-14：新增 `GET /documents` 聚合查询、接口测试和前端文档列表页；代码尚未执行 pytest、`npm run build` 或真实数据验证，因此暂未标记完成。
- 2026-07-14：用户确认 `npm run build`、前端真实页面连接和文档上传入库均验证通过；前端初始化与文档上传入口完成。
- 2026-07-14：新增文档上传页面、multipart/form-data API 调用、文件格式与大小前端校验和入库结果展示；尚未执行 `npm run build` 或真实文件上传，因此暂未标记完成。
- 2026-07-14：记录前端开发服务器显示已启动但页面无法访问的问题；原因为 Vite 只监听 `::1`，而本机 IPv6 环回连接异常，解决方式是让 Vite 明确监听 `127.0.0.1`。
- 2026-07-14：用户已完成 `npm install` 并确认 Vite 开发服务器和前端页面可以访问；尚未执行 `npm run build` 或确认 `/api/health` 代理链路，因此前端初始化暂未标记完成。
- 2026-07-14：用户确认 `uv sync --extra dev`、知识库问答离线测试、全部回归测试和 Swagger 真实 `POST /answer` 调用均正常；阶段 2 验证完成。
- 2026-07-14：新增 `POST /answer`、LangChain 问答提示词链、`qwen3.7-plus` 非思考模式调用、空知识库分支和离线测试；尚未执行依赖同步、pytest 或真实模型调用，因此暂未标记完成。
- 2026-07-14：用户通过 Swagger 使用真实问题验证 `POST /search`，确认召回结果正常；最小向量检索闭环验证完成。
- 2026-07-14：用户执行 pytest，`POST /search` 的问题参数校验、pgvector 余弦距离查询、来源响应测试及现有测试全部通过；尚未使用真实问题验证检索结果。
- 2026-07-14：根据用户提供的数据库截图确认，`document_chunks` 中存在同一文档的 6 条切片，`chunk_index` 为 0～5，`content`、`embedding` 和 `created_at` 均已成功写入；正式向量入库闭环验证完成。
- 2026-07-13：用户执行 pytest，正式入库接口、embedding 分批、模型失败不写库、数据库失败整体回滚及现有测试全部通过；下一步验证真实百炼调用和 PostgreSQL 向量写入。
- 2026-07-13：新增 `POST /documents` 正式入库接口、异步会话依赖、批量 embedding 编排、事务回滚测试和操作文档；尚未执行 pytest，也未调用真实百炼或写入数据库，因此暂未标记完成。
- 2026-07-13：用户运行 pytest，文档上传预览测试及现有测试全部通过。
- 2026-07-13：用户通过 Swagger 调用 `POST /documents/preview` 上传真实文件，接口和切片响应符合预期。
- 2026-07-13：新增 `POST /documents/preview`，支持最大 2 MB 的 UTF-8 txt／md 文件，返回切片预览；增加格式、大小、编码、空文件和长文本测试，尚未执行验证。
- 2026-07-11：用户执行 pytest，新增文本切片测试及现有测试全部通过。
- 2026-07-11：新增 LangChain `RecursiveCharacterTextSplitter` 中文切片服务，默认 800 字符、120 字符重叠，并增加空文本、短文本、长文本、重叠和参数边界测试；尚未执行验证，因此未标记完成。
- 2026-07-11：用户运行 pytest，新增 embedding 模拟测试及现有测试均通过。
- 2026-07-11：用户执行 `uv run python -m scripts.check_embedding`，模型名称、1024 维向量长度和向量样例输出均符合预期；确认本地配置可以真实调用百炼。
- 2026-07-11：实现百炼 `text-embedding-v4` 异步客户端，固定单批最多 10 条、1024 维输出，并增加不访问网络的模拟测试和手动真实调用脚本；尚未执行验证，因此未标记完成。
- 2026-07-11：用户首次执行 `uv run alembic upgrade head` 时，Windows 使用 GBK 读取含 UTF-8 中文注释的 `alembic.ini`，触发 `UnicodeDecodeError`；已将该配置文件改为纯 ASCII，等待重新执行迁移验证。
- 2026-07-11：用户重新执行迁移成功，并在 `rag_db` 中确认 `documents`、`document_chunks` 和 `alembic_version` 三张表存在。
- 2026-07-11：用户执行 `uv run pytest`，5 个测试全部通过；结果为 `5 passed, 1 warning`，warning 仍来自 FastAPI/Starlette TestClient 依赖层，不影响当前模型和接口测试。
- 2026-07-11：确定阿里云百炼 `text-embedding-v4`，项目统一使用 1024 维 dense embedding。
- 2026-07-11：新增 `documents`、`document_chunks` SQLAlchemy 模型和第一次 Alembic 迁移；尚未执行 `uv sync`、pytest 或真实数据库迁移，因此未标记完成。
- 2026-07-10：用户执行 `uv run pytest` 两次，结果分别为 `2 passed, 1 warning`；warning 来自 FastAPI/Starlette TestClient 依赖层，不影响测试通过。
- 2026-07-10：用户启动后端并访问 `/health/db`，真实返回 `{"status":"ok","database":"connected"}`，确认 FastAPI、SQLAlchemy、asyncpg 和 PostgreSQL 链路连通。
- 2026-07-10：增加 `.env.example`，并让后端自动读取项目根目录的 `.env`；真实密码仍由 `.gitignore` 排除，本次等待用户执行测试验证。
- 2026-07-10：按照新手向注释规范补充数据库配置、异步引擎、健康检查接口及测试代码说明；本次只修改注释，未执行测试。
- 2026-07-10：细化新手向代码注释规范，要求生成代码解释文件职责、导入来源、自定义变量、关键语法、调用流程和数据流向。
- 2026-07-10：补充学习型协作分工：Codex 负责编写和解释，用户负责执行依赖同步、测试与服务启动命令；未收到真实结果前不得标记完成。
- 2026-07-09：文档初始化完成并已提交。
- 2026-07-09：后端文件已创建；本机 `python` 和 `py` 命令不可用，暂无法运行 FastAPI 或 pytest。
- 2026-07-09：后端骨架注释补充完成；本次为注释和说明更新，未新增运行验证项。
- 2026-07-09：已核对 uv 官方项目命令，确认 `uv sync --extra dev` 和 `uv run ...` 是当前后端文档采用的命令形式。
- 2026-07-09：当前终端执行 `uv --version` 失败，说明 uv 未安装或未加入 PATH；已在文档中补充 uv 安装和检查命令。
- 2026-07-09：修复后端 pytest 导入路径配置，并使用 `uv run pytest` 完成验证；结果为 `1 passed, 1 warning`，warning 为 FastAPI/Starlette TestClient 依赖层弃用提醒，不影响当前健康检查测试通过。
- 2026-07-09：新增 PostgreSQL + pgvector 的 `docker-compose.yml`，`docker compose config` 已通过；`docker compose up -d postgres` 因 Docker daemon 未运行失败，容器启动待复验。
- 2026-07-10：用户已启动 PostgreSQL + pgvector，并通过 VS Code 插件连接 `rag_db`；`vector` 扩展可用性查询结果符合预期。

## 待确认

- 暂无。
